import styled from 'styled-components';

export const StandAloneContainer = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: var(--color--bg-primary);
`;

export const StandAloneInnerContainer = styled.div`
  max-width: 768px;
  margin: 0 auto;
`;
